# Public env variables for SvelteKit must be prefixed with PUBLIC_
# Set the base URL for your backend API (e.g., https://api.example.com or /api)
PUBLIC_API_BASE_URL=/api

# When true, the frontend will use a built-in mock API using localStorage (no backend required)
PUBLIC_USE_MOCK_API=true
